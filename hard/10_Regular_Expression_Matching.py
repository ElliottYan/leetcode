"""
Need to consider the * symbol

if there is no * symbol, it will be much easier.
so first think about how to implement without * 

recursive method:
each time match the next word and pattern

special case:
1. pattern is empty
2. string is empty
"""
class Solution:
    def isMatch(self, s, p):
        """
        :type s: str
        :type p: str
        :rtype: bool
        """
        # first p
        if not p:
            return True
        # in the case of p is not
        if not s:
            return False




        isMatch()

    # def match_
        
